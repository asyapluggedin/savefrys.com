comments:
  allowedFields: ["name", "email", "store", "message"]
  branch: "main"
  commitMessage: "comment!"
  filename: "entries-{@timestamp}" 
  format: "yaml"
  name: "savefrys.com"
  path: "_data/comments"
  moderation: false
  requiredFields: ["name", "store", "message"]

